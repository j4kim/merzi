# merzi

ics based freetime seeker for cff employees.

## Set up

Install dependencies:

    composer install

    npm install

Create config file:

    cp config.example.json config.json

Serve locally:

    composer serve